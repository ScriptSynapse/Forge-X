# FORGE-X — Frontend

React + TypeScript + Tailwind CSS + React Router.

> Status: PLACEHOLDER. Not implemented yet — this phase only establishes
> the project skeleton. Implementation begins in a later step.

Planned structure (not yet created):
- `src/pages/` — route-level views (Cases, Evidence, Custody, Examinations, Reports, Audit)
- `src/components/` — reusable UI components
- `src/api/` — typed API client for the backend
- `src/types/` — shared TypeScript types
